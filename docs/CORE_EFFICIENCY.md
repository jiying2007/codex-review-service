# Core efficiency ownership

Codex Review Service delegates generic token estimation, risk scoring, adaptive budgets, model selection, byte planning and concurrent token reservation to the exact-pinned Codex Safe Core. Service-owned code retains only GitLab/SQLite/incremental-review concerns such as changed-path selection and finding carry-forward.
