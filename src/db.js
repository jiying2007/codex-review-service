'use strict';

const fs = require('node:fs');
const path = require('node:path');
const { DatabaseSync } = require('node:sqlite');
const { CURRENT_SCHEMA_VERSION, FINDING_RESOLUTIONS, migrateDatabase } = require('./db-migrations');

const SCHEMA_VERSION = CURRENT_SCHEMA_VERSION;
const TERMINAL_JOB_STATUSES = Object.freeze(['pass','needs_attention','blocked','incomplete','failed','superseded','cancelled','unauthorized','duplicate','skipped']);
const TERMINAL_PUBLICATION_STATUSES = Object.freeze(['published','failed','cancelled']);
function isoAfter(ms){return new Date(Date.now()+Math.max(0,ms)).toISOString();}
function json(value){return JSON.stringify(value??null);}
function parseJson(value){try{return JSON.parse(value);}catch{return null;}}
function cardTextForDb(value,max=120){return String(value??'').replace(/[\u0000-\u001f\u007f]/g,' ').replace(/\s+/g,' ').trim().slice(0,max);}

class Store{
  constructor(dbPath,{migrationHooks={}}={}){this.dbPath=dbPath;this.migrationHooks=migrationHooks;if(dbPath!==':memory:')fs.mkdirSync(path.dirname(dbPath),{recursive:true,mode:0o700});this.db=new DatabaseSync(dbPath);try{this.db.exec('PRAGMA journal_mode=WAL; PRAGMA synchronous=FULL; PRAGMA foreign_keys=ON; PRAGMA busy_timeout=5000;');this.initializeSchema();}catch(error){try{this.db.close();}catch{}throw error;}}
  withTransaction(fn){this.db.exec('BEGIN IMMEDIATE');try{const value=fn();this.db.exec('COMMIT');return value;}catch(error){try{this.db.exec('ROLLBACK');}catch{}throw error;}}
  initializeSchema(){
    const current=Number(this.db.prepare('PRAGMA user_version').get().user_version||0);
    const existing=this.db.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%' ORDER BY name").all().map(row=>row.name);
    if(current===SCHEMA_VERSION)return;
    if(current===5||current===6||current===7){this.lastMigration=migrateDatabase(this.db,this.dbPath,current,this.migrationHooks);return;}
    if(current!==0||existing.length){const error=new Error(`Unsupported database schema ${current}; expected fresh database, Schema 5/6/7 migration source, or Schema ${SCHEMA_VERSION}`);error.code='EDBSCHEMA';throw error;}
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS webhook_events(id INTEGER PRIMARY KEY AUTOINCREMENT,webhook_id TEXT NOT NULL UNIQUE,event_type TEXT NOT NULL,project_id INTEGER,mr_iid INTEGER,received_at TEXT NOT NULL,processed_at TEXT);
      CREATE TABLE IF NOT EXISTS review_jobs(id INTEGER PRIMARY KEY AUTOINCREMENT,project_id INTEGER NOT NULL,mr_iid INTEGER NOT NULL,base_sha TEXT NOT NULL DEFAULT '',start_sha TEXT NOT NULL DEFAULT '',head_sha TEXT NOT NULL DEFAULT '',dedupe_key TEXT NOT NULL,status TEXT NOT NULL,trigger TEXT NOT NULL,attempt INTEGER NOT NULL DEFAULT 0,error_code TEXT,created_at TEXT NOT NULL,started_at TEXT,finished_at TEXT,requested_by_user_id INTEGER,request_webhook_id TEXT,source_branch TEXT NOT NULL DEFAULT '',available_at TEXT NOT NULL DEFAULT '',status_project_id INTEGER,pipeline_id INTEGER,trace_id TEXT NOT NULL DEFAULT '',UNIQUE(project_id,mr_iid,dedupe_key));
      CREATE TABLE IF NOT EXISTS review_runs(id INTEGER PRIMARY KEY AUTOINCREMENT,job_id INTEGER NOT NULL REFERENCES review_jobs(id) ON DELETE CASCADE,verdict TEXT NOT NULL,summary TEXT NOT NULL,coverage_complete INTEGER NOT NULL,finding_count INTEGER NOT NULL,codex_version TEXT,codex_model TEXT NOT NULL DEFAULT '',duration_ms INTEGER NOT NULL,created_at TEXT NOT NULL,policy_source TEXT NOT NULL DEFAULT 'service-default',policy_fingerprint TEXT NOT NULL DEFAULT '<none>',chunk_count INTEGER NOT NULL DEFAULT 0,rejected_finding_count INTEGER NOT NULL DEFAULT 0,truncated_finding_count INTEGER NOT NULL DEFAULT 0,input_tokens INTEGER NOT NULL DEFAULT 0,cached_input_tokens INTEGER NOT NULL DEFAULT 0,cache_write_input_tokens INTEGER NOT NULL DEFAULT 0,output_tokens INTEGER NOT NULL DEFAULT 0,reasoning_output_tokens INTEGER NOT NULL DEFAULT 0,receipt_json TEXT NOT NULL DEFAULT '',receipt_fingerprint TEXT NOT NULL DEFAULT '');
      CREATE TABLE IF NOT EXISTS review_findings(id INTEGER PRIMARY KEY AUTOINCREMENT,run_id INTEGER NOT NULL REFERENCES review_runs(id) ON DELETE CASCADE,fingerprint TEXT NOT NULL,severity TEXT NOT NULL,category TEXT NOT NULL,file TEXT NOT NULL,line INTEGER NOT NULL,end_line INTEGER NOT NULL,title TEXT NOT NULL,description TEXT NOT NULL,suggestion TEXT NOT NULL,confidence REAL NOT NULL,discussion_id TEXT,side TEXT NOT NULL DEFAULT 'new',anchor_hash TEXT NOT NULL DEFAULT '',UNIQUE(run_id,fingerprint));
      CREATE TABLE IF NOT EXISTS publication_outbox(id INTEGER PRIMARY KEY AUTOINCREMENT,run_id INTEGER REFERENCES review_runs(id) ON DELETE CASCADE,project_id INTEGER NOT NULL,mr_iid INTEGER NOT NULL,action_type TEXT NOT NULL,dedupe_key TEXT NOT NULL UNIQUE,payload_json TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',attempt INTEGER NOT NULL DEFAULT 0,available_at TEXT NOT NULL,remote_id TEXT,error_code TEXT,created_at TEXT NOT NULL,started_at TEXT,finished_at TEXT);
      CREATE TABLE IF NOT EXISTS notification_outbox(id INTEGER PRIMARY KEY AUTOINCREMENT,run_id INTEGER REFERENCES review_runs(id) ON DELETE CASCADE,project_id INTEGER NOT NULL DEFAULT 0,mr_iid INTEGER NOT NULL DEFAULT 0,route_name TEXT NOT NULL,provider TEXT NOT NULL,secret_ref TEXT NOT NULL,event_type TEXT NOT NULL,dedupe_key TEXT NOT NULL UNIQUE,payload_json TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',attempt INTEGER NOT NULL DEFAULT 0,available_at TEXT NOT NULL,remote_id TEXT,error_code TEXT,created_at TEXT NOT NULL,started_at TEXT,finished_at TEXT,aggregate_key TEXT GENERATED ALWAYS AS (json_extract(payload_json,'$._aggregateKey')) VIRTUAL,aggregate_until TEXT GENERATED ALWAYS AS (json_extract(payload_json,'$._aggregateUntil')) VIRTUAL,operation_type TEXT GENERATED ALWAYS AS (json_extract(payload_json,'$._operationType')) VIRTUAL,status_card_job_id INTEGER GENERATED ALWAYS AS (json_extract(payload_json,'$._statusCardJobId')) VIRTUAL);
      CREATE TABLE IF NOT EXISTS flow_state(project_id INTEGER NOT NULL,flow_type TEXT NOT NULL,external_id TEXT NOT NULL,ref TEXT NOT NULL DEFAULT '',previous_status TEXT NOT NULL DEFAULT '',current_status TEXT NOT NULL,revision INTEGER NOT NULL DEFAULT 1,delivery_identity TEXT NOT NULL DEFAULT '',updated_at TEXT NOT NULL,PRIMARY KEY(project_id,flow_type,external_id));
      CREATE TABLE IF NOT EXISTS review_status_cards(job_id INTEGER NOT NULL REFERENCES review_jobs(id) ON DELETE CASCADE,route_name TEXT NOT NULL,provider TEXT NOT NULL,message_id TEXT NOT NULL DEFAULT '',status TEXT NOT NULL DEFAULT 'pending',error_code TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,PRIMARY KEY(job_id,route_name));
    `);
    this.db.exec(`
      CREATE TABLE IF NOT EXISTS schema_migrations(version INTEGER PRIMARY KEY,applied_at TEXT NOT NULL,backup_path TEXT NOT NULL DEFAULT '');
      CREATE TABLE IF NOT EXISTS finding_resolutions(id INTEGER PRIMARY KEY AUTOINCREMENT,finding_id INTEGER NOT NULL REFERENCES review_findings(id) ON DELETE CASCADE,resolution TEXT NOT NULL CHECK(resolution IN ('fixed','false_positive','accepted_risk','duplicate','obsolete','not_applicable','policy_exception')),note TEXT NOT NULL DEFAULT '',actor TEXT NOT NULL DEFAULT '',created_at TEXT NOT NULL);
      CREATE INDEX IF NOT EXISTS idx_finding_resolutions_finding ON finding_resolutions(finding_id,id);
      CREATE INDEX IF NOT EXISTS idx_finding_resolutions_created ON finding_resolutions(created_at,id);
    `);
    this.db.exec(`CREATE INDEX IF NOT EXISTS idx_review_jobs_status_available ON review_jobs(status,available_at,id);CREATE INDEX IF NOT EXISTS idx_review_jobs_mr ON review_jobs(project_id,mr_iid,id);CREATE INDEX IF NOT EXISTS idx_review_jobs_branch_head ON review_jobs(project_id,source_branch,head_sha,created_at);CREATE INDEX IF NOT EXISTS idx_review_runs_job ON review_runs(job_id,id);CREATE INDEX IF NOT EXISTS idx_review_runs_created ON review_runs(created_at,id);CREATE INDEX IF NOT EXISTS idx_webhooks_processed ON webhook_events(processed_at,received_at);CREATE INDEX IF NOT EXISTS idx_outbox_status_available ON publication_outbox(status,available_at,id);CREATE INDEX IF NOT EXISTS idx_outbox_run ON publication_outbox(run_id,id);CREATE INDEX IF NOT EXISTS idx_notification_status_available ON notification_outbox(status,available_at,id);CREATE INDEX IF NOT EXISTS idx_notification_run ON notification_outbox(run_id,id);CREATE INDEX IF NOT EXISTS idx_notification_aggregate ON notification_outbox(status,aggregate_key,aggregate_until,id);CREATE INDEX IF NOT EXISTS idx_notification_operation ON notification_outbox(status,operation_type,status_card_job_id,route_name,id);CREATE INDEX IF NOT EXISTS idx_flow_state_updated ON flow_state(updated_at,project_id,flow_type);CREATE INDEX IF NOT EXISTS idx_review_status_cards_status ON review_status_cards(status,updated_at);PRAGMA user_version=${SCHEMA_VERSION};`);
    this.db.prepare('INSERT OR REPLACE INTO schema_migrations(version,applied_at,backup_path) VALUES(?,?,?)').run(SCHEMA_VERSION,new Date().toISOString(),'fresh');
  }
  schemaVersion(){return Number(this.db.prepare('PRAGMA user_version').get().user_version||0);}
  synchronousMode(){return Number(this.db.prepare('PRAGMA synchronous').get().synchronous);}
  ping(){return Number(this.db.prepare('SELECT 1 AS ok').get().ok)===1;}
  recordWebhook({webhookId,eventType,projectId=null,mrIid=null}){try{this.db.prepare('INSERT INTO webhook_events(webhook_id,event_type,project_id,mr_iid,received_at) VALUES(?,?,?,?,?)').run(webhookId,eventType,projectId,mrIid,new Date().toISOString());return true;}catch(error){if(String(error.message).includes('UNIQUE constraint failed'))return false;throw error;}}
  markWebhookProcessed(webhookId){this.db.prepare('UPDATE webhook_events SET processed_at=? WHERE webhook_id=?').run(new Date().toISOString(),webhookId);}
  forgetWebhook(webhookId){this.db.prepare('DELETE FROM webhook_events WHERE webhook_id=? AND processed_at IS NULL').run(webhookId);}
  queueDepth(){return Number(this.db.prepare("SELECT COUNT(*) count FROM review_jobs WHERE status='queued'").get().count);}
  publicationDepth(){return Number(this.db.prepare("SELECT COUNT(*) count FROM publication_outbox WHERE status='pending'").get().count);}
  notificationDepth(){return Number(this.db.prepare("SELECT COUNT(*) count FROM notification_outbox WHERE status='pending'").get().count);}
  relatedMrIid(projectId,branch,headSha='',maxAgeMs=86400000){const cutoff=new Date(Date.now()-Math.max(60000,Number(maxAgeMs)||86400000)).toISOString(),sha=String(headSha||'').trim(),row=this.db.prepare("SELECT mr_iid FROM review_jobs WHERE project_id=? AND source_branch=? AND created_at>=? AND status NOT IN ('cancelled','superseded','skipped') AND (?='' OR head_sha=?) ORDER BY id DESC LIMIT 1").get(Number(projectId),String(branch||''),cutoff,sha,sha);return Number(row?.mr_iid||0)||null;}
  enqueueReviewStatusCards(jobId,actions=[]){return this.withTransaction(()=>{const now=new Date().toISOString(),card=this.db.prepare("INSERT OR IGNORE INTO review_status_cards(job_id,route_name,provider,status,created_at,updated_at) VALUES(?,?,?,'pending',?,?)"),notification=this.db.prepare("INSERT OR IGNORE INTO notification_outbox(run_id,project_id,mr_iid,route_name,provider,secret_ref,event_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(NULL,?,?,?,?,?,?,?,?,'pending',?,?)");let count=0;for(const action of actions){card.run(Number(jobId),action.routeName,action.provider,now,now);const result=notification.run(action.payload?.projectId||0,action.payload?.mrIid||0,action.routeName,action.provider,action.secretRef,action.eventType,action.dedupeKey,json(action.payload),now,now);count+=Number(result.changes||0);}return count;});}
  reviewStatusCard(jobId,routeName){return this.db.prepare('SELECT * FROM review_status_cards WHERE job_id=? AND route_name=?').get(Number(jobId),String(routeName))||null;}
  terminalReviewStatusCards(){return this.db.prepare("SELECT c.*,j.project_id,j.mr_iid,j.head_sha,j.source_branch,j.status job_status,j.error_code job_error FROM review_status_cards c JOIN review_jobs j ON j.id=c.job_id WHERE c.status='delivered' AND j.status IN ('superseded','cancelled','failed') AND NOT EXISTS(SELECT 1 FROM notification_outbox n WHERE n.route_name=c.route_name AND n.status IN ('pending','delivering') AND n.operation_type='status_update' AND n.status_card_job_id=c.job_id) ORDER BY c.updated_at LIMIT 100").all();}
  finishReviewStatusCard(jobId,routeName,messageId,status='delivered'){this.db.prepare('UPDATE review_status_cards SET message_id=?,status=?,error_code=NULL,updated_at=? WHERE job_id=? AND route_name=?').run(String(messageId||''),status,new Date().toISOString(),Number(jobId),String(routeName));}
  failReviewStatusCard(jobId,routeName,errorCode){this.db.prepare("UPDATE review_status_cards SET status='failed',error_code=?,updated_at=? WHERE job_id=? AND route_name=?").run(String(errorCode||'ENOTIFY'),new Date().toISOString(),Number(jobId),String(routeName));}
  coalesceCommitPushNotifications(projectId,ref,windowMs=30000){return this.withTransaction(()=>{const rows=this.db.prepare("SELECT * FROM notification_outbox WHERE project_id=? AND event_type='gitlab.push.committed' AND status='pending' AND aggregate_key IS NOT NULL ORDER BY aggregate_key,id").all(Number(projectId)),groups=new Map();for(const row of rows){const payload=parseJson(row.payload_json);if(payload?.ref!==String(ref||''))continue;const list=groups.get(row.aggregate_key)||[];list.push({...row,payload});groups.set(row.aggregate_key,list);}let merged=0;for(const list of groups.values()){if(list.length<2)continue;const newest=list.at(-1),cutoff=Date.parse(newest.created_at)-Math.max(0,Number(windowMs)||0),selected=list.filter(row=>Date.parse(row.created_at)>=cutoff);if(selected.length<2)continue;const oldest=selected[0],commits=selected.flatMap(row=>row.payload.commits||[]),commitCount=selected.reduce((sum,row)=>sum+Number(row.payload.commitCount||0),0),payload={...newest.payload,beforeSha:oldest.payload.beforeSha||newest.payload.beforeSha,commitCount,commits:commits.slice(-20),displayedCommitCount:Math.min(20,commits.length),omittedCommitCount:Math.max(0,commitCount-Math.min(20,commits.length)),_aggregatedEvents:selected.length};this.db.prepare('UPDATE notification_outbox SET payload_json=? WHERE id=?').run(json(payload),newest.id);const ids=selected.slice(0,-1).map(row=>row.id),placeholders=ids.map(()=>'?').join(',');this.db.prepare(`DELETE FROM notification_outbox WHERE id IN (${placeholders}) AND status='pending'`).run(...ids);merged+=ids.length;}return merged;});}
  finalizeCommitPushAggregation(projectId,ref){return this.withTransaction(()=>{const rows=this.db.prepare("SELECT id,payload_json,aggregate_until FROM notification_outbox WHERE project_id=? AND event_type='gitlab.push.committed' AND status='pending' AND aggregate_key IS NOT NULL").all(Number(projectId));let changed=0;for(const row of rows){const payload=parseJson(row.payload_json);if(payload?.ref!==String(ref||''))continue;if(payload.beforeSha&&payload.afterSha&&payload.url)payload.url=String(payload.url).replace(/\/\-\/compare\/[^?]+/,`/-/compare/${payload.beforeSha}...${payload.afterSha}`);this.db.prepare('UPDATE notification_outbox SET payload_json=?,available_at=? WHERE id=?').run(json(payload),row.aggregate_until||new Date().toISOString(),row.id);changed++;}return changed;});}
  coalescePendingChangeActivities(windowMs=30000){return this.withTransaction(()=>{const rows=this.db.prepare("SELECT * FROM notification_outbox WHERE status='pending' AND mr_iid>0 AND aggregate_key IS NOT NULL AND event_type IN ('gitlab.push.committed','gitlab.pipeline.failed','gitlab.pipeline.succeeded','review.completed','review.blocked') ORDER BY aggregate_key,id").all(),groups=new Map();for(const row of rows){if(row.operation_type==='status_create')continue;const payload=parseJson(row.payload_json),list=groups.get(row.aggregate_key)||[];list.push({...row,payload});groups.set(row.aggregate_key,list);}let merged=0;for(const list of groups.values()){if(list.length<2)continue;const latest=list.at(-1),selected=list.filter(row=>Date.parse(row.created_at)>=Date.parse(latest.created_at)-windowMs);if(selected.length<2)continue;const survivor=selected.filter(row=>row.operation_type==='status_update').at(-1)||latest,activity=selected.map(row=>({type:row.event_type,status:row.payload.status||row.payload.verdict||'',title:cardTextForDb(row.payload.title||row.payload.ref||'',120),url:String(row.payload.url||'')})),payload={...survivor.payload,_activity:activity,_aggregatedEvents:selected.length};this.db.prepare('UPDATE notification_outbox SET payload_json=?,available_at=? WHERE id=?').run(json(payload),survivor.aggregate_until||new Date().toISOString(),survivor.id);const ids=selected.filter(row=>row.id!==survivor.id).map(row=>row.id),marks=ids.map(()=>'?').join(',');this.db.prepare(`DELETE FROM notification_outbox WHERE id IN (${marks}) AND status='pending'`).run(...ids);merged+=ids.length;}return merged;});}
  enqueue({projectId,mrIid,baseSha='',startSha='',headSha='',sourceBranch='',trigger,dedupeKey,requestedByUserId=null,requestWebhookId=null,maxQueueDepth=Infinity,delayMs=0,traceId=''}){if(this.queueDepth()>=maxQueueDepth){const error=new Error('Review queue is full');error.code='EQUEUEFULL';error.status=503;throw error;}const now=new Date().toISOString(),availableAt=isoAfter(delayMs);return this.withTransaction(()=>{if(headSha)this.db.prepare(`UPDATE review_jobs SET status='superseded',finished_at=? WHERE project_id=? AND mr_iid=? AND status='queued' AND (head_sha<>'' AND head_sha<>? OR (?<>'' AND start_sha<>'' AND start_sha<>?))`).run(now,projectId,mrIid,headSha,startSha,startSha);try{const result=this.db.prepare(`INSERT INTO review_jobs(project_id,mr_iid,base_sha,start_sha,head_sha,dedupe_key,status,trigger,created_at,available_at,requested_by_user_id,request_webhook_id,source_branch,trace_id) VALUES(?,?,?,?,?,?,'queued',?,?,?,?,?,?,?)`).run(projectId,mrIid,baseSha,startSha,headSha,dedupeKey,trigger,now,availableAt,requestedByUserId,requestWebhookId,sourceBranch,traceId);return Number(result.lastInsertRowid);}catch(error){if(String(error.message).includes('UNIQUE constraint failed'))return null;throw error;}});}
  bindJobSnapshot(id,{baseSha,startSha,headSha,sourceBranch,statusProjectId=null,pipelineId=null}){const now=new Date().toISOString();return this.withTransaction(()=>{const job=this.db.prepare('SELECT * FROM review_jobs WHERE id=?').get(id);if(!job)return{status:'missing'};if(job.trigger!=='command'){const duplicate=this.db.prepare(`SELECT id FROM review_jobs WHERE project_id=? AND mr_iid=? AND start_sha=? AND head_sha=? AND id<>? AND trigger<>'command' ORDER BY id LIMIT 1`).get(job.project_id,job.mr_iid,startSha,headSha,id);if(duplicate){this.db.prepare("UPDATE review_jobs SET status='duplicate',head_sha=?,base_sha=?,start_sha=?,source_branch=?,finished_at=? WHERE id=?").run(headSha,baseSha,startSha,sourceBranch||'',now,id);return{status:'duplicate',duplicateId:duplicate.id};}}this.db.prepare('UPDATE review_jobs SET base_sha=?,start_sha=?,head_sha=?,source_branch=?,status_project_id=?,pipeline_id=? WHERE id=?').run(baseSha||'',startSha||'',headSha,sourceBranch||'',statusProjectId,pipelineId,id);this.db.prepare(`UPDATE review_jobs SET status='superseded',finished_at=? WHERE project_id=? AND mr_iid=? AND id<>? AND status='queued' AND (head_sha<>'' AND head_sha<>? OR (?<>'' AND start_sha<>'' AND start_sha<>?))`).run(now,job.project_id,job.mr_iid,id,headSha,startSha,startSha);return{status:'bound'};});}
  recoverInterruptedJobs(){return this.db.prepare("UPDATE review_jobs SET status='queued',started_at=NULL,error_code='ESERVICERESTART',available_at=? WHERE status='running'").run(new Date().toISOString()).changes;}
  recoverPublications(){return this.db.prepare("UPDATE publication_outbox SET status='pending',started_at=NULL,error_code='ESERVICERESTART',available_at=? WHERE status='publishing'").run(new Date().toISOString()).changes;}
  recoverNotifications(){return this.db.prepare("UPDATE notification_outbox SET status='pending',started_at=NULL,error_code='ESERVICERESTART',available_at=? WHERE status='delivering'").run(new Date().toISOString()).changes;}
  claimNext(){return this.withTransaction(()=>{const now=new Date().toISOString(),row=this.db.prepare(`SELECT j.* FROM review_jobs j WHERE j.status='queued' AND j.available_at<=? AND NOT EXISTS(SELECT 1 FROM review_jobs r WHERE r.project_id=j.project_id AND r.mr_iid=j.mr_iid AND r.status='running') ORDER BY j.id LIMIT 1`).get(now);if(!row)return null;const result=this.db.prepare("UPDATE review_jobs SET status='running',started_at=?,attempt=attempt+1 WHERE id=? AND status='queued'").run(now,row.id);return result.changes===1?this.getJob(row.id):null;});}
  claimPublication(){return this.withTransaction(()=>{const now=new Date().toISOString(),row=this.db.prepare(`SELECT p.* FROM publication_outbox p WHERE p.status='pending' AND p.available_at<=? AND NOT EXISTS(SELECT 1 FROM publication_outbox prior WHERE prior.run_id=p.run_id AND p.run_id IS NOT NULL AND prior.id<p.id AND prior.status IN ('pending','publishing')) ORDER BY p.id LIMIT 1`).get(now);if(!row)return null;const result=this.db.prepare("UPDATE publication_outbox SET status='publishing',started_at=?,attempt=attempt+1 WHERE id=? AND status='pending'").run(now,row.id);if(result.changes!==1)return null;const value=this.db.prepare('SELECT * FROM publication_outbox WHERE id=?').get(row.id);value.payload=parseJson(value.payload_json);return value;});}
  claimNotification(){return this.withTransaction(()=>{const now=new Date().toISOString(),row=this.db.prepare("SELECT * FROM notification_outbox WHERE status='pending' AND available_at<=? ORDER BY id LIMIT 1").get(now);if(!row)return null;const result=this.db.prepare("UPDATE notification_outbox SET status='delivering',started_at=?,attempt=attempt+1 WHERE id=? AND status='pending'").run(now,row.id);if(result.changes!==1)return null;const value=this.db.prepare('SELECT * FROM notification_outbox WHERE id=?').get(row.id);value.payload=parseJson(value.payload_json);return value;});}
  getJob(id){return this.db.prepare('SELECT * FROM review_jobs WHERE id=?').get(id)||null;}
  retryJob(id,errorCode,delayMs){this.db.prepare("UPDATE review_jobs SET status='queued',error_code=?,started_at=NULL,available_at=? WHERE id=? AND status='running'").run(errorCode,isoAfter(delayMs),id);}
  requeueRunningJob(id,errorCode='ESHUTDOWN'){this.db.prepare("UPDATE review_jobs SET status='queued',error_code=?,started_at=NULL,available_at=? WHERE id=? AND status='running'").run(errorCode,new Date().toISOString(),id);}
  finishJob(id,status,errorCode=null){this.db.prepare('UPDATE review_jobs SET status=?,error_code=?,finished_at=? WHERE id=?').run(status,errorCode,new Date().toISOString(),id);}
  finishJobWithNotifications(id,status,errorCode,actions=[]){return this.withTransaction(()=>{const now=new Date().toISOString();this.db.prepare('UPDATE review_jobs SET status=?,error_code=?,finished_at=? WHERE id=?').run(status,errorCode,now,id);const insert=this.db.prepare("INSERT OR IGNORE INTO notification_outbox(run_id,project_id,mr_iid,route_name,provider,secret_ref,event_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(NULL,?,?,?,?,?,?,?,?,'pending',?,?)");for(const action of actions||[])insert.run(action.payload?.projectId||0,action.payload?.mrIid||0,action.routeName,action.provider,action.secretRef,action.eventType,action.dedupeKey,json(action.payload),now,now);return actions.length;});}
  cancelMergeRequest(projectId,mrIid,status='cancelled'){return this.db.prepare("UPDATE review_jobs SET status=?,finished_at=? WHERE project_id=? AND mr_iid=? AND status IN ('queued','running')").run(status,new Date().toISOString(),projectId,mrIid).changes;}
  enqueuePublication({runId=null,projectId,mrIid,type,dedupeKey,payload,delayMs=0}){const now=new Date().toISOString();try{const result=this.db.prepare(`INSERT INTO publication_outbox(run_id,project_id,mr_iid,action_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(?,?,?,?,?,?,'pending',?,?)`).run(runId,projectId,mrIid,type,dedupeKey,json(payload),isoAfter(delayMs),now);return Number(result.lastInsertRowid);}catch(error){if(String(error.message).includes('UNIQUE constraint failed'))return null;throw error;}}
  enqueueNotification({runId=null,routeName,provider,secretRef,eventType,dedupeKey,payload,delayMs=0}){const now=new Date().toISOString();try{const result=this.db.prepare("INSERT INTO notification_outbox(run_id,project_id,mr_iid,route_name,provider,secret_ref,event_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(?,?,?,?,?,?,?,?,?,'pending',?,?)").run(runId,payload?.projectId||0,payload?.mrIid||0,routeName,provider,secretRef,eventType,dedupeKey,json(payload),isoAfter(delayMs),now);return Number(result.lastInsertRowid);}catch(error){if(String(error.message).includes('UNIQUE constraint failed'))return null;throw error;}}
  saveRunWithOutbox(jobId,review,durationMs,policy,actions,receiptProjection=null,notificationActions=[]){return this.withTransaction(()=>{const usage=review.usage||{},run=this.db.prepare(`INSERT INTO review_runs(job_id,verdict,summary,coverage_complete,finding_count,codex_version,codex_model,duration_ms,created_at,policy_source,policy_fingerprint,chunk_count,rejected_finding_count,truncated_finding_count,input_tokens,cached_input_tokens,cache_write_input_tokens,output_tokens,reasoning_output_tokens,receipt_json,receipt_fingerprint) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(jobId,review.verdict,review.summary,review.coverageComplete?1:0,review.findings.length,review.codexVersion||null,review.codexModel||'',durationMs,new Date().toISOString(),policy.source,policy.fingerprint,review.chunkCount||0,review.rejectedFindingCount||0,review.truncatedFindingCount||0,usage.inputTokens||0,usage.cachedInputTokens||0,usage.cacheWriteInputTokens||0,usage.outputTokens||0,usage.reasoningOutputTokens||0,receiptProjection?json(receiptProjection.receipt):'',receiptProjection?.fingerprint||''),runId=Number(run.lastInsertRowid),insertFinding=this.db.prepare(`INSERT INTO review_findings(run_id,fingerprint,severity,category,file,line,end_line,title,description,suggestion,confidence,side,anchor_hash) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`);for(const f of review.findings)insertFinding.run(runId,f.fingerprint,f.severity,f.category,f.file,f.line,f.endLine,f.title,f.description,f.suggestion,f.confidence,f.side,f.anchorHash||'');const insertAction=this.db.prepare(`INSERT OR IGNORE INTO publication_outbox(run_id,project_id,mr_iid,action_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(?,?,?,?,?,?,'pending',?,?)`),insertNotification=this.db.prepare("INSERT OR IGNORE INTO notification_outbox(run_id,project_id,mr_iid,route_name,provider,secret_ref,event_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(?,?,?,?,?,?,?,?,?,'pending',?,?)"),now=new Date().toISOString();for(const action of actions||[])insertAction.run(runId,action.projectId,action.mrIid,action.type,action.dedupeKey,json(action.payload),now,now);for(const action of notificationActions||[])insertNotification.run(runId,action.payload?.projectId||0,action.payload?.mrIid||0,action.routeName,action.provider,action.secretRef,action.eventType,action.dedupeKey,json(action.payload),now,now);return runId;});}
  receiptForRun(runId){const row=this.db.prepare('SELECT receipt_json,receipt_fingerprint FROM review_runs WHERE id=?').get(runId);return row?{receipt:parseJson(row.receipt_json),fingerprint:row.receipt_fingerprint}:null;}
  findingsForRun(runId){return this.db.prepare('SELECT * FROM review_findings WHERE run_id=? ORDER BY id').all(runId);}
  priorFindings(projectId,mrIid,beforeRunId=Number.MAX_SAFE_INTEGER){return this.db.prepare(`SELECT f.*,r.id run_id,j.id job_id FROM review_findings f JOIN review_runs r ON r.id=f.run_id JOIN review_jobs j ON j.id=r.job_id WHERE j.project_id=? AND j.mr_iid=? AND r.id<? ORDER BY r.id DESC,f.id`).all(projectId,mrIid,beforeRunId);}
  setDiscussionIdByFingerprint(runId,fingerprint,discussionId){this.db.prepare('UPDATE review_findings SET discussion_id=? WHERE run_id=? AND fingerprint=?').run(discussionId,runId,fingerprint);}
  finishNotification(id,remoteId=null){this.db.prepare("UPDATE notification_outbox SET status='delivered',remote_id=?,error_code=NULL,finished_at=? WHERE id=?").run(remoteId,new Date().toISOString(),id);}
  retryNotification(id,errorCode,delayMs){this.db.prepare("UPDATE notification_outbox SET status='pending',error_code=?,started_at=NULL,available_at=? WHERE id=? AND status='delivering'").run(errorCode,isoAfter(delayMs),id);}
  deferNotification(id,errorCode,delayMs){this.db.prepare("UPDATE notification_outbox SET status='pending',attempt=MAX(0,attempt-1),error_code=?,started_at=NULL,available_at=? WHERE id=? AND status='delivering'").run(errorCode,isoAfter(delayMs),id);}
  failNotification(id,errorCode){this.db.prepare("UPDATE notification_outbox SET status='failed',error_code=?,finished_at=? WHERE id=?").run(errorCode,new Date().toISOString(),id);}
  finishPublication(id,remoteId=null){this.db.prepare("UPDATE publication_outbox SET status='published',remote_id=?,error_code=NULL,finished_at=? WHERE id=?").run(remoteId,new Date().toISOString(),id);}
  retryPublication(id,errorCode,delayMs){this.db.prepare("UPDATE publication_outbox SET status='pending',error_code=?,started_at=NULL,available_at=? WHERE id=? AND status='publishing'").run(errorCode,isoAfter(delayMs),id);}
  failPublication(id,errorCode){this.db.prepare("UPDATE publication_outbox SET status='failed',error_code=?,finished_at=? WHERE id=?").run(errorCode,new Date().toISOString(),id);}
  cancelPublication(id,errorCode='ECANCELLED'){this.db.prepare("UPDATE publication_outbox SET status='cancelled',error_code=?,finished_at=? WHERE id=?").run(errorCode,new Date().toISOString(),id);}
  tokenUsageSince(projectId,sinceIso){const row=this.db.prepare(`SELECT COALESCE(SUM(r.input_tokens+r.output_tokens),0) tokens FROM review_runs r JOIN review_jobs j ON j.id=r.job_id WHERE j.project_id=? AND r.created_at>=?`).get(projectId,sinceIso);return Number(row.tokens||0);}
  recordFindingResolution(findingId,resolution,{note='',actor=''}={}){const id=Number(findingId),value=String(resolution||'');if(!Number.isInteger(id)||id<=0){const error=new Error('findingId must be a positive integer');error.code='EFINDINGRESOLUTION';throw error;}if(!FINDING_RESOLUTIONS.includes(value)){const error=new Error(`Unsupported finding resolution: ${value}`);error.code='EFINDINGRESOLUTION';throw error;}const text=String(note||'').trim(),who=String(actor||'').trim();if(text.length>2000||who.length>255||/[\r\n\0]/.test(who)){const error=new Error('Finding resolution metadata is invalid');error.code='EFINDINGRESOLUTION';throw error;}if(!this.db.prepare('SELECT id FROM review_findings WHERE id=?').get(id)){const error=new Error(`Finding ${id} does not exist`);error.code='EFINDINGNOTFOUND';throw error;}const result=this.db.prepare('INSERT INTO finding_resolutions(finding_id,resolution,note,actor,created_at) VALUES(?,?,?,?,?)').run(id,value,text,who,new Date().toISOString());return Number(result.lastInsertRowid);}
  findingResolutionHistory(findingId){return this.db.prepare('SELECT * FROM finding_resolutions WHERE finding_id=? ORDER BY id').all(Number(findingId));}
  latestFindingResolution(findingId){return this.db.prepare('SELECT * FROM finding_resolutions WHERE finding_id=? ORDER BY id DESC LIMIT 1').get(Number(findingId))||null;}
  resolutionMetrics(since=''){const rows=since?this.db.prepare('SELECT resolution,COUNT(*) count FROM finding_resolutions WHERE created_at>=? GROUP BY resolution ORDER BY resolution').all(String(since)):this.db.prepare('SELECT resolution,COUNT(*) count FROM finding_resolutions GROUP BY resolution ORDER BY resolution').all();return Object.freeze(Object.fromEntries(rows.map(row=>[row.resolution,Number(row.count)])));}
  transitionFlowState({projectId,flowType,externalId,ref='',status,deliveryIdentity=''},actionFactory=null){const pid=Number(projectId),type=String(flowType||''),external=String(externalId||''),next=String(status||''),now=new Date().toISOString();if(!Number.isInteger(pid)||pid<=0||!type||!external||!next){const error=new Error('Flow state identity is invalid');error.code='EFLOWSTATE';throw error;}return this.withTransaction(()=>{const row=this.db.prepare('SELECT * FROM flow_state WHERE project_id=? AND flow_type=? AND external_id=?').get(pid,type,external),finish=transition=>{if(!transition.changed||typeof actionFactory!=='function')return transition;const actions=actionFactory(Object.freeze({...transition}))||[],insert=this.db.prepare("INSERT OR IGNORE INTO notification_outbox(run_id,project_id,mr_iid,route_name,provider,secret_ref,event_type,dedupe_key,payload_json,status,available_at,created_at) VALUES(NULL,?,?,?,?,?,?,?,?,'pending',?,?)");for(const action of actions)insert.run(action.payload?.projectId||0,action.payload?.mrIid||0,action.routeName,action.provider,action.secretRef,action.eventType,action.dedupeKey,json(action.payload),now,now);return{...transition,notificationCount:actions.length};};if(!row){this.db.prepare('INSERT INTO flow_state(project_id,flow_type,external_id,ref,previous_status,current_status,revision,delivery_identity,updated_at) VALUES(?,?,?,?,?,?,?,?,?)').run(pid,type,external,String(ref||''),'',next,1,String(deliveryIdentity||''),now);return finish({changed:true,previousStatus:'',revision:1});}if(row.current_status===next){this.db.prepare('UPDATE flow_state SET ref=?,delivery_identity=?,updated_at=? WHERE project_id=? AND flow_type=? AND external_id=?').run(String(ref||''),String(deliveryIdentity||''),now,pid,type,external);return{changed:false,previousStatus:next,revision:Number(row.revision)};}const revision=Number(row.revision)+1;this.db.prepare('UPDATE flow_state SET ref=?,previous_status=current_status,current_status=?,revision=?,delivery_identity=?,updated_at=? WHERE project_id=? AND flow_type=? AND external_id=?').run(String(ref||''),next,revision,String(deliveryIdentity||''),now,pid,type,external);return finish({changed:true,previousStatus:String(row.current_status||''),revision});});}
  stats(){const jobs=Object.fromEntries(this.db.prepare('SELECT status,COUNT(*) count FROM review_jobs GROUP BY status').all().map(r=>[r.status,Number(r.count)])),publications=Object.fromEntries(this.db.prepare('SELECT status,COUNT(*) count FROM publication_outbox GROUP BY status').all().map(r=>[r.status,Number(r.count)])),notifications=Object.fromEntries(this.db.prepare('SELECT status,COUNT(*) count FROM notification_outbox GROUP BY status').all().map(r=>[r.status,Number(r.count)])),usage=this.db.prepare('SELECT COALESCE(SUM(input_tokens),0) input,COALESCE(SUM(cached_input_tokens),0) cached,COALESCE(SUM(output_tokens),0) output,COALESCE(SUM(reasoning_output_tokens),0) reasoning FROM review_runs').get();return{jobs,publications,notifications,flowStates:Number(this.db.prepare('SELECT COUNT(*) count FROM flow_state').get().count),webhookCount:Number(this.db.prepare('SELECT COUNT(*) count FROM webhook_events').get().count),findings:Number(this.db.prepare('SELECT COUNT(*) count FROM review_findings').get().count),schemaVersion:this.schemaVersion(),usage};}
  prune({dataRetentionDays,webhookRetentionDays}){const jobCutoff=new Date(Date.now()-dataRetentionDays*86400000).toISOString(),webhookCutoff=new Date(Date.now()-webhookRetentionDays*86400000).toISOString();return this.withTransaction(()=>{const webhooks=this.db.prepare('DELETE FROM webhook_events WHERE processed_at IS NOT NULL AND received_at<?').run(webhookCutoff).changes,flowStates=this.db.prepare('DELETE FROM flow_state WHERE updated_at<?').run(jobCutoff).changes,placeholders=TERMINAL_JOB_STATUSES.map(()=>'?').join(','),jobs=this.db.prepare(`DELETE FROM review_jobs WHERE status IN (${placeholders}) AND finished_at IS NOT NULL AND finished_at<?`).run(...TERMINAL_JOB_STATUSES,jobCutoff).changes;return{webhooks,flowStates,jobs};});}
  checkpoint(){try{return this.db.prepare('PRAGMA wal_checkpoint(PASSIVE)').get();}catch{return null;}}
  close(){this.db.close();}
}
module.exports={Store,SCHEMA_VERSION,FINDING_RESOLUTIONS,TERMINAL_JOB_STATUSES,TERMINAL_PUBLICATION_STATUSES};
