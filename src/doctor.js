'use strict';
const{loadConfig}=require('./config');const{Store,SCHEMA_VERSION}=require('./db');const{GitLabClient}=require('./gitlab');const{ProjectScopeManager}=require('./project-scope');const{probeCodexCapabilities}=require('./codex');const{contract}=require('./product-contract');
async function main(){const checks={product:{ok:true,...contract,node:process.version}};let store;try{const config=loadConfig();checks.config={ok:config.configSchemaVersion===contract.configSchemaVersion,file:config.configFilePath,schemaVersion:config.configSchemaVersion,expectedSchemaVersion:contract.configSchemaVersion,deployment:config.runnerMode==='isolated'?'hardened':'standard'};store=new Store(config.dbPath);const quick=store.db.prepare('PRAGMA quick_check').get()?.quick_check||'unknown',foreign=store.db.prepare('PRAGMA foreign_key_check').all();checks.database={ok:store.ping()&&store.schemaVersion()===SCHEMA_VERSION&&SCHEMA_VERSION===contract.databaseSchemaVersion&&store.synchronousMode()===2&&quick==='ok'&&!foreign.length,schemaVersion:store.schemaVersion(),expectedSchemaVersion:contract.databaseSchemaVersion,synchronous:store.synchronousMode(),quickCheck:quick,foreignKeyViolations:foreign.length,publicationDepth:store.publicationDepth(),notificationDepth:store.notificationDepth()};try{const capability=await probeCodexCapabilities(config,true);checks.codex={ok:true,mode:config.runnerMode,version:capability.version,versionMatched:capability.versionMatched,versionPolicy:config.codexVersionPolicy};if(config.codexVersionPolicy==='warn'&&capability.versionMatched===false)checks.codex.warning='Codex version does not match codex.allowedVersionPattern';}catch(error){checks.codex={ok:false,code:error.code||'ECODEX'};}try{const gitlab=new GitLabClient(config),version=await gitlab.getVersion(),capabilities=await gitlab.getCapabilities(),scope=new ProjectScopeManager(gitlab,config),resolved=await scope.refresh();checks.gitlab={ok:true,version:version?.version||'unknown',minimumVersion:contract.minimumGitLabVersion,recommendedPolicy:contract.recommendedGitLabPolicy,profile:capabilities.profile,diffCompleteness:capabilities.diffCompleteness,circuit:gitlab.health().circuit};checks.projectScope={ok:resolved.healthy,mode:resolved.mode,explicitProjects:resolved.explicitProjects,groups:resolved.groups,discoveredProjects:resolved.discoveredProjects,totalProjects:resolved.totalProjects};}catch(error){checks.gitlab={ok:false,code:error.code||'EGITLAB',status:error.status||null,version:error.version||null,minimumVersion:error.minimumVersion||contract.minimumGitLabVersion};checks.projectScope={ok:false,code:error.code||'EPROJECTSCOPE'};}checks.publication={ok:true,mode:'durable-outbox',publishers:config.publisherConcurrency,maxAttempts:config.maxPublishAttempts};checks.notifications={ok:true,enabled:config.notificationEnabled,notifiers:config.notificationEnabled?config.notificationConcurrency:0,maxAttempts:config.maxNotificationAttempts};checks.context={ok:true,enabled:config.contextEnabled,maxBytes:config.maxContextBytes,maxFiles:config.maxContextFiles};checks.budget={ok:true,mrMaxTokens:config.mrMaxTokenBudget,projectDailyTokens:config.projectDailyTokenBudget};}catch(error){checks.config={ok:false,code:error.code||'ECONFIG',message:error.message};}finally{try{store?.close();}catch{}}const ok=Object.values(checks).every(check=>check.ok!==false);process.stdout.write(`${JSON.stringify({ok,checks},null,2)}\n`);if(!ok)process.exitCode=1;}
main().catch(error=>{process.stderr.write(`${JSON.stringify({ok:false,error:error.code||'EDOCTOR'})}\n`);process.exitCode=1;});
