'use strict';
const{loadConfig}=require('./config');const{Store}=require('./db');const{installFairScheduling}=require('./fair-scheduler');const{GitLabClient}=require('./gitlab');const{ProjectScopeManager,applyProjectScope}=require('./project-scope');const{ReviewService}=require('./service');const{Publisher}=require('./publication');const{Telemetry}=require('./telemetry');const{createHttpServer}=require('./http');const{probeCodexCapabilities}=require('./codex');
function log(level,value){const record=typeof value==='object'&&value?value:{message:String(value)};process.stdout.write(`${JSON.stringify({ts:new Date().toISOString(),level,...record})}\n`);}
async function listen(server,port,host){await new Promise((resolve,reject)=>{const onError=e=>{server.off('listening',onListening);reject(e);},onListening=()=>{server.off('error',onError);resolve();};server.once('error',onError);server.once('listening',onListening);server.listen(port,host);});}
async function main(){const startedAt=Date.now();let config=loadConfig();const logger={info:v=>log('info',v),warn:v=>log('warn',v),error:v=>log('error',v)},telemetry=new Telemetry({logger,endpoint:config.otelEndpoint,serviceName:config.otelServiceName,timeoutMs:config.otelExportTimeoutMs});const capability=await probeCodexCapabilities(config);logger.info({event:'codex_ready',mode:config.runnerMode,version:capability.version,versionMatched:capability.versionMatched,runnerCapability:capability.runnerCapability||null});if(capability.versionMatched===false)logger.warn({event:'codex_version_policy_warning',version:capability.version});const store=new Store(config.dbPath),scheduler=installFairScheduling(store),recoveredJobs=store.recoverInterruptedJobs(),recoveredPublications=store.recoverPublications();if(recoveredJobs)logger.info({event:'jobs_recovered',count:recoveredJobs});if(recoveredPublications)logger.info({event:'publications_recovered',count:recoveredPublications});const gitlab=new GitLabClient(config),scopeManager=new ProjectScopeManager(gitlab,config);await scopeManager.refresh();config=applyProjectScope(config,scopeManager);logger.info({event:'project_scope_ready',mode:scopeManager.mode,...scopeManager.stats});const service=new ReviewService({config,store,gitlab,logger,telemetry}),publisher=new Publisher({config,store,gitlab,logger});service.startWorkers();publisher.start();const server=createHttpServer({config,store,service,publisher,gitlab,telemetry,logger,startedAt});await listen(server,config.port,config.host);logger.info({event:'service_started',host:config.host,port:config.port,deployment:config.runnerMode==='isolated'?'hardened':'standard',workers:config.workerConcurrency,publishers:config.publisherConcurrency,schemaVersion:store.schemaVersion(),synchronous:store.synchronousMode(),scheduler:'project-fair'});const maintenance=setInterval(()=>{try{const pruned=store.prune(config);store.checkpoint();if(pruned.webhooks||pruned.jobs)logger.info({event:'data_pruned',...pruned});}catch(error){logger.warn({event:'maintenance_failed',code:error.code||'EMAINTENANCE'});}},config.maintenanceIntervalMs);maintenance.unref?.();const reconcileOnce=()=>service.reconcile().then(result=>{if(result.enqueued)logger.info({event:'reconcile_enqueued',...result});}).catch(error=>logger.warn({event:'reconcile_failed',code:error.code||'ERECONCILE'}));const periodic=()=>config.gitlabGroups.length?scopeManager.refresh().then(snapshot=>{logger.info({event:'project_scope_refreshed',mode:snapshot.mode,totalProjects:snapshot.totalProjects,discoveredProjects:snapshot.discoveredProjects});return reconcileOnce();}).catch(error=>logger.error({event:'project_scope_refresh_failed',code:error.code||'EPROJECTSCOPE'})):reconcileOnce();const reconcile=setInterval(periodic,config.reconcileIntervalMs);reconcile.unref?.();setTimeout(()=>reconcileOnce(),Math.min(5000,config.pollIntervalMs)).unref?.();let shuttingDown=false;const shutdown=async signal=>{if(shuttingDown)return;shuttingDown=true;logger.info({event:'shutdown',signal,scheduler:scheduler.snapshot()});clearInterval(maintenance);clearInterval(reconcile);await new Promise(resolve=>server.close(resolve));await service.stop();await publisher.stop();await telemetry.flush();store.checkpoint();store.close();};process.on('SIGTERM',()=>{shutdown('SIGTERM').catch(()=>process.exit(1));});process.on('SIGINT',()=>{shutdown('SIGINT').catch(()=>process.exit(1));});process.on('unhandledRejection',error=>logger.error({event:'unhandled_rejection',code:error?.code||'EUNHANDLED'}));}
main().catch(error=>{log('error',{event:'startup_failed',code:error.code||'ESTART',message:error.message});process.exitCode=1;});
