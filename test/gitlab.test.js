'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{GitLabClient,discussionResolved}=require('../src/gitlab');function config(){return{gitlabApiUrl:'https://gitlab.example.test/api/v4',gitlabToken:'token',statusName:'codex-review',statusTargetUrl:'',gitlabRequestTimeoutMs:1000,gitlabMaxPages:3,gitlabStatusRetries:2};}test('pagination follows X-Next-Page and reports completeness',async()=>{const oldFetch=global.fetch,pages=[];global.fetch=async url=>{const page=new URL(url).searchParams.get('page');pages.push(page);return new Response(JSON.stringify(page==='1'?[{id:1}]:[{id:2}]),{status:200,headers:page==='1'?{'x-next-page':'2'}:{}});};try{const result=await new GitLabClient(config()).listMergeRequestDiffs(1,2);assert.deepEqual(pages,['1','2']);assert.equal(result.complete,true);assert.equal(result.items.length,2);}finally{global.fetch=oldFetch;}});test('commit status includes source ref',async()=>{const oldFetch=global.fetch;let requestUrl='';global.fetch=async url=>(requestUrl=String(url),new Response('{}',{status:201}));try{await new GitLabClient(config()).setCommitStatus(1,'abc','success','ok','feat/test');assert.equal(new URL(requestUrl).searchParams.get('ref'),'feat/test');}finally{global.fetch=oldFetch;}});test('discussion resolved state reads resolvable note',()=>{assert.equal(discussionResolved({notes:[{resolvable:false},{resolvable:true,resolved:true}]}),true);assert.equal(discussionResolved({notes:[{resolvable:true,resolved:false}]}),false);assert.equal(discussionResolved(null),null);});
