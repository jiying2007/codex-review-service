'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{parseProjectAllowlist,validateSigningToken,normalizeBaseUrl}=require('../src/config');test('parses explicit project allowlist and wildcard',()=>{assert.deepEqual([...parseProjectAllowlist('1, 2,2')],[1,2]);assert.equal(parseProjectAllowlist('*'),null);assert.throws(()=>parseProjectAllowlist(''),/required/);assert.throws(()=>parseProjectAllowlist('abc'),/invalid/);});test('validates standard webhook signing token shape',()=>{const token=`whsec_${Buffer.alloc(32,7).toString('base64')}`;assert.equal(validateSigningToken(token),token);assert.throws(()=>validateSigningToken('whsec_YQ=='),/32 bytes/);assert.throws(()=>validateSigningToken('bad'),/whsec_/);});test('normalizes safe GitLab base URLs',()=>{assert.equal(normalizeBaseUrl('https://gitlab.example.test/','X'),'https://gitlab.example.test');assert.throws(()=>normalizeBaseUrl('https://u:p@gitlab.example.test','X'),/without credentials/);});
