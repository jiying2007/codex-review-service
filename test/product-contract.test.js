'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const contract=require('../product-contract.json');const{CONFIG_SCHEMA_VERSION}=require('../src/config');const{SCHEMA_VERSION}=require('../src/db');
test('machine product contract matches runtime constants and examples',()=>{assert.equal(CONFIG_SCHEMA_VERSION,contract.configSchemaVersion);assert.equal(SCHEMA_VERSION,contract.databaseSchemaVersion);assert.equal(require('../package.json').version,contract.serviceVersion);for(const file of['config.example.json','deploy/systemd/config.example.json','deploy/docker/config.example.json'])assert.equal(JSON.parse(fs.readFileSync(path.resolve(file),'utf8')).schemaVersion,contract.configSchemaVersion);});
