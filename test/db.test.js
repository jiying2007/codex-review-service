'use strict';const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),os=require('node:os'),path=require('node:path');const{DatabaseSync}=require('node:sqlite');const{Store,SCHEMA_VERSION}=require('../src/db');function withStore(fn){const dir=fs.mkdtempSync(path.join(os.tmpdir(),'codex-review-db-')),store=new Store(path.join(dir,'test.sqlite'));try{return fn(store);}finally{store.close();fs.rmSync(dir,{recursive:true,force:true});}}test('migrates a legacy review_jobs table without data loss',()=>{const dir=fs.mkdtempSync(path.join(os.tmpdir(),'codex-review-migrate-')),dbPath=path.join(dir,'test.sqlite'),legacy=new DatabaseSync(dbPath);legacy.exec(`CREATE TABLE review_jobs(id INTEGER PRIMARY KEY,project_id INTEGER NOT NULL,mr_iid INTEGER NOT NULL,base_sha TEXT NOT NULL DEFAULT '',head_sha TEXT NOT NULL,dedupe_key TEXT NOT NULL,status TEXT NOT NULL,trigger TEXT NOT NULL,attempt INTEGER NOT NULL DEFAULT 0,error_code TEXT,created_at TEXT NOT NULL,started_at TEXT,finished_at TEXT,UNIQUE(project_id,mr_iid,dedupe_key));`);legacy.prepare("INSERT INTO review_jobs(project_id,mr_iid,head_sha,dedupe_key,status,trigger,created_at) VALUES(1,2,'h','head:h','queued','open',?)").run(new Date().toISOString());legacy.close();const store=new Store(dbPath);try{assert.equal(store.schemaVersion(),SCHEMA_VERSION);const row=store.db.prepare('SELECT start_sha,available_at FROM review_jobs WHERE id=1').get();assert.equal(row.start_sha,'');assert.ok(row.available_at);}finally{store.close();fs.rmSync(dir,{recursive:true,force:true});}});test('serializes jobs for same MR while allowing different MRs',()=>withStore(store=>{store.enqueue({projectId:1,mrIid:2,headSha:'h',trigger:'command',dedupeKey:'c1',maxQueueDepth:10});store.enqueue({projectId:1,mrIid:2,headSha:'h',trigger:'command',dedupeKey:'c2',maxQueueDepth:10});store.enqueue({projectId:1,mrIid:3,headSha:'z',trigger:'open',dedupeKey:'z',maxQueueDepth:10});assert.equal(store.claimNext().mr_iid,2);assert.equal(store.claimNext().mr_iid,3);assert.equal(store.claimNext(),null);}));test('binds snapshots and deduplicates automatic jobs',()=>withStore(store=>{const a=store.enqueue({projectId:1,mrIid:2,headSha:'',trigger:'open',dedupeKey:'event:a',maxQueueDepth:10});store.bindJobSnapshot(a,{baseSha:'b',startSha:'s',headSha:'h',sourceBranch:'feat'});const b=store.enqueue({projectId:1,mrIid:2,startSha:'s',headSha:'h',trigger:'reconcile',dedupeKey:'snapshot:s:h',maxQueueDepth:10});assert.equal(store.bindJobSnapshot(b,{baseSha:'b',startSha:'s',headSha:'h',sourceBranch:'feat'}).status,'duplicate');assert.equal(store.getJob(b).status,'duplicate');}));test('delays retry availability and reports schema stats',()=>withStore(store=>{const id=store.enqueue({projectId:1,mrIid:2,headSha:'h',trigger:'open',dedupeKey:'h',maxQueueDepth:10});assert.equal(store.claimNext().id,id);store.retryJob(id,'E',60000);assert.equal(store.claimNext(),null);assert.equal(store.stats().schemaVersion,SCHEMA_VERSION);}));test('new snapshot waits for active same-MR job to exit',()=>withStore(store=>{const oldId=store.enqueue({projectId:1,mrIid:2,startSha:'s1',headSha:'h1',trigger:'open',dedupeKey:'snapshot:s1:h1',maxQueueDepth:10});assert.equal(store.claimNext().id,oldId);const newId=store.enqueue({projectId:1,mrIid:2,startSha:'s2',headSha:'h2',trigger:'update',dedupeKey:'snapshot:s2:h2',maxQueueDepth:10});assert.equal(store.getJob(oldId).status,'running');assert.equal(store.claimNext(),null);store.finishJob(oldId,'superseded');assert.equal(store.claimNext().id,newId);}));test('target start-SHA change supersedes older queued snapshot with same head',()=>withStore(store=>{const oldId=store.enqueue({projectId:1,mrIid:2,startSha:'s1',headSha:'h',trigger:'reconcile',dedupeKey:'snapshot:s1:h',maxQueueDepth:10});const newId=store.enqueue({projectId:1,mrIid:2,startSha:'s2',headSha:'h',trigger:'reconcile',dedupeKey:'snapshot:s2:h',maxQueueDepth:10});assert.equal(store.getJob(oldId).status,'superseded');assert.equal(store.getJob(newId).status,'queued');}));
