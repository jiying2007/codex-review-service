'use strict';const test=require('node:test'),assert=require('node:assert/strict'),crypto=require('node:crypto');const{verifyWebhook,normalizeEvent}=require('../src/webhook');function config(){const key=Buffer.alloc(32,3);return{webhookSigningToken:`whsec_${key.toString('base64')}`,webhookSecretToken:'legacy',webhookMaxSkewSeconds:300,webhookExpectedInstance:'https://gitlab.example.test',requireInstanceHeader:true,botUsername:'codex-review-bot',triggerOnOpen:true,triggerOnPush:true,triggerOnReopen:true,gitlabProjectAllowlist:new Set([7]),_key:key};}test('verifies signed webhook including expected instance',()=>{const c=config(),body='{}',id='abc',timestamp='1000',sig='v1,'+crypto.createHmac('sha256',c._key).update(`${id}.${timestamp}.${body}`).digest('base64');const result=verifyWebhook({'webhook-id':id,'webhook-timestamp':timestamp,'webhook-signature':sig,'x-gitlab-instance':'https://gitlab.example.test/'},body,c,1000*1000);assert.equal(result.ok,true);assert.equal(result.mode,'hmac');});test('rejects wrong GitLab instance before token fallback',()=>{const result=verifyWebhook({'x-gitlab-event-uuid':'id','x-gitlab-token':'legacy','x-gitlab-instance':'https://evil.test'},'{}',config());assert.equal(result.ok,false);assert.equal(result.reason,'wrong_instance');});test('MR update reviews only source-code updates with oldrev',()=>{const c=config(),base={object_kind:'merge_request',project:{id:7}};assert.equal(normalizeEvent({...base,object_attributes:{iid:9,action:'update',last_commit:{id:'h'}}},{},c).shouldReview,false);assert.equal(normalizeEvent({...base,object_attributes:{iid:9,action:'update',oldrev:'old',last_commit:{id:'h'}}},{},c).shouldReview,true);});test('manual command requires create action, user id, and allowed project',()=>{const c=config(),payload={object_kind:'note',project:{id:7},user:{id:12,username:'alice'},merge_request:{iid:9},object_attributes:{action:'create',note:'/codex review'}};assert.equal(normalizeEvent(payload,{},c).shouldReview,true);assert.equal(normalizeEvent({...payload,object_attributes:{action:'update',note:'/codex review'}},{},c).shouldReview,false);assert.equal(normalizeEvent({...payload,project:{id:8}},{},c).shouldReview,false);});
