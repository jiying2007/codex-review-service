# Hardened Runner environment. Never place GitLab credentials here.
# The systemd unit explicitly reads non-secret Codex/Runner settings from
# /etc/codex-review/config.json. Do not duplicate that path here.

# Optional API-key mode instead of managed `codex login` state.
# OPENAI_API_KEY=replace-me
